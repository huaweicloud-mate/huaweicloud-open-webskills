---
name: huawei-cloud-web-form
description: 通用网页表单的开发规范与工作流，覆盖表单目标与字段设计、字段校验（手机号/邮箱/微信号/身份证等）、提交流程（防重复提交、加载与禁用状态、成功/失败反馈）、防刷与安全（蜜罐、限流、token、验证码）、埋点统计（表单曝光、字段错误、提交点击、提交成功/失败）、可访问性与上线检查清单。适用于所有网页表单场景：推广报名、留资、登录/注册、数据录入、搜索、问卷、活动、配置表单等。当用户需要实现、修改或评审网页表单时使用，也可用于生成表单模板代码。
---

# 表单开发 Skill

面向通用网页的表单开发规范，适用场景包括推广报名、留资、登录/注册、数据录入、搜索、问卷、活动、配置等。按以下流程工作，细节参考对应资源文件。

## 工作流

### 1. 明确表单目标与字段
- 先确认表单目标（报名、留资、注册、数据录入、搜索、配置等）与业务指标（转化、留存、注册完成率、数据质量等），再定字段，只保留必要字段。
- 区分必填/选填、字段类型（文本、手机、邮箱、下拉、单选、多选、文件、隐私勾选）。
- 确认数据接收方：提交接口地址、字段名映射、数据用途，与后端对齐字段名。

### 2. 定义字段校验
- 按 `references/validation-rules.md` 逐字段配置校验规则与错误提示。
- 校验时机：失焦时校验单字段，提交时校验全部字段；错误提示需具体、友好。
- 前端校验只是体验优化，服务端必须做同样校验，规则保持一致。
- 防抖与节流：实时输入校验用防抖（默认 300ms）；提交防连点用按钮禁用 + 请求 token，不用节流（会吞掉合法提交）；高频埋点上报用节流。

### 3. 实现表单 UI
- 原生页面复制 `assets/form-template/`；Vue 3 项目复制 `assets/form-template-vue/SignupForm.vue`，均按需裁剪。
- 表单元素必须绑定 label，错误提示通过 aria-describedby/role 与输入框关联。
- 提交按钮提供明确文案（如「立即报名」「注册」「提交」），点击后进入加载态并禁用。

### 4. 接入提交流程
- 按 `references/submission-flow.md` 实现：序列化、加载态、成功/失败反馈、超时处理。
- 防止重复提交：提交中禁用按钮，配合请求唯一 token；以服务端幂等为准。
- 提交成功：显示成功反馈并触发转化埋点；失败时保留已填内容并提供重试。

### 5. 防刷与安全
- 按 `references/submission-flow.md` 实现蜜罐字段、限流、token 校验。
- 按风险分级配置防护强度：低风险（搜索、配置等）做基础限流；中风险（留资、数据录入）加图形验证码或滑块；高风险（登录/注册、活动报名、支付类）叠加手机号/设备维度限频与去重。
- 前端不出现签名密钥或管理员凭据；文本字段入库前转义，防 XSS/注入。

### 6. 埋点与统计
- 事件命名统一：`form_view`、`form_start`、`field_error`、`submit_click`、`submit_success`、`submit_fail`。
- 推广表单以提交成功为转化指标；注册/登录关注注册完成率、登录成功率；数据录入关注录入完成率与放弃率；搜索关注搜索发起与结果选择。页面需透传渠道来源参数（如 utm_source）；不上报敏感字段。

### 7. 可访问性
- 每个输入控件有可见 label；错误提示用 aria-describedby 关联，输入框错误态用 aria-invalid。
- 支持键盘操作；必填项用 required 标注；文本与背景对比度满足 WCAG AA。

### 8. 上线自检清单
- [ ] 必填与格式校验全部覆盖，错误提示无错别字
- [ ] 手机号等格式符合目标地区规则
- [ ] 重复点击不会产生重复提交
- [ ] 提交成功、业务失败、网络异常、超时均有反馈
- [ ] 蜜罐与限流已生效，服务端完成重复校验
- [ ] 埋点事件在测试环境验证上报成功
- [ ] 隐私政策与同意勾选符合合规要求

## 资源

- `references/validation-rules.md`：各字段类型的校验规则、正则与错误提示，实施校验时读取。
- `references/submission-flow.md`：提交流程、防重复提交、防刷与埋点细则，接入接口时读取。
- `assets/form-template/`：原生 HTML/CSS/JS 表单模板，可直接复制使用，按需替换为任意场景字段与文案。
- `assets/form-template-vue/SignupForm.vue`：Vue 3（`<script setup>`）单文件组件版模板，复制到 Vue 3 + Vite 项目中使用，逻辑与原生版一致；模板以推广报名为例，字段、校验与文案可替换为其他场景。
